name: mobilelab_flutter_example
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobilelab_flutter:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
